<Application x:Class="Chess.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Chess"
             StartupUri="Views/Windows/MainWindow.xaml">
    <Application.Resources>  
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Themes/Standard_Theme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <Cursor x:Key="Grab">Assets/Cursors/Grab.cur</Cursor>
            <Cursor x:Key="Grabbing">Assets/Cursors/Grabbing.cur</Cursor>
            <Style TargetType="FrameworkElement">
                <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            </Style>
            <Style TargetType="Button">
                <Setter Property="Cursor" Value="Hand"/>
            </Style>
            <Style TargetType="Image">
                <Setter Property="RenderOptions.BitmapScalingMode" Value="HighQuality"/>
                <Setter Property="SnapsToDevicePixels" Value="True"/>
                <Setter Property="UseLayoutRounding" Value="True"/>
                <Setter Property="Stretch" Value="Uniform"/>
            </Style>
            <Style x:Key="Cell" TargetType="FrameworkElement">
                <Setter Property="Grid.Column" Value="{Binding Position.Column}"/>
                <Setter Property="Visibility" Value="{Binding Visibility}"/>
                <Setter Property="Grid.Row" Value="{Binding Position.Row}"/>
                <Setter Property="Margin" Value="-.1"/>
            </Style>
            <ItemsPanelTemplate x:Key="Grid_8x8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                </Grid>
            </ItemsPanelTemplate>
            <Style x:Key="Board_8x8" TargetType="UserControl">
                <Style.Setters>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="UserControl">
                                <Grid>
                                    <ItemsControl ItemsSource="{Binding HighLightManage}">
                                        <ItemsControl.ItemsPanel>
                                            <StaticResource ResourceKey="Grid_8x8"/>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemContainerStyle>
                                            <StaticResource ResourceKey="Cell"/>
                                        </ItemsControl.ItemContainerStyle>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Rectangle>
                                                    <Rectangle.Fill>
                                                        <MultiBinding Converter="{StaticResource HighLightBrushConverter}">
                                                            <Binding Path="IsCheck"/>
                                                            <Binding Path="IsClick"/>
                                                            <Binding Path="HighLightType"/>
                                                            <Binding Path="EvaluateType"/>
                                                        </MultiBinding>
                                                    </Rectangle.Fill>
                                                </Rectangle>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style.Setters>
            </Style>
            <Style x:Key="Transparent_TextBox" TargetType="TextBox">
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="#303030"/>
                <Setter Property="FontFamily" Value="Segoe UI"/>
                <Setter Property="TextWrapping" Value="Wrap"/>
                <Setter Property="FontWeight" Value="Medium"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="Padding" Value="0"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TextBox">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>   
    </Application.Resources>
</Application>
